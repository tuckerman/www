# www
Personal website

## Setup

Clone the repo:

```bash
$ git clone git@github.com:tuckerman/www.git
```

Init submodules:

```bash
$ git submodule update --init --recursive
```

Start dev server:

```bash
$ hugo server
```
